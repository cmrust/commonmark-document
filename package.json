{
  "name": "commonmark-document",
  "version": "0.0.1",
  "description": "Application for serving Commonmark-compliant markdown in a 'document' format",
  "scripts": {
    "start": "node app.js"
  },
  "dependencies": {
    "commonmark": "^0.9.0"
  }
}